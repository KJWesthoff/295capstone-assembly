# Production Environment Variables for AWS Deployment

# Backend Configuration
JWT_SECRET=your-super-secure-jwt-secret-here
PYTHONUNBUFFERED=1

# CORS Configuration
FRONTEND_URL=https://myapp.example.com
ADDITIONAL_CORS_ORIGINS=https://api.myapp.example.com,https://cdn.myapp.example.com

# Database (if using RDS instead of Redis)
# DATABASE_URL=postgresql://user:pass@rds-endpoint/dbname

# AWS Configuration (for ECS/Fargate)
AWS_DEFAULT_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key

# Security
ALLOWED_HOSTS=api.myapp.example.com,myapp.example.com