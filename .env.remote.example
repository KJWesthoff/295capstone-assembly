# =============================================================================
# VentiAPI Scanner - Production/Remote Environment Template
# =============================================================================
# This file serves as a template for .env.remote
# In production, this file is auto-generated by Terraform user-data.sh
# For manual deployments, copy this to .env.remote and fill in values
# =============================================================================

# Environment
ENVIRONMENT=production
DEBUG=false
NODE_ENV=production

# Security - CHANGE THESE IN PRODUCTION
JWT_SECRET=your-secure-jwt-secret-here
ADMIN_USERNAME=your-admin-username
ADMIN_PASSWORD=your-secure-password

# Redis (internal Docker network)
REDIS_URL=redis://redis:6379
REDIS_HOST=redis
REDIS_PORT=6379

# Database (internal Docker network)
DATABASE_URL=postgresql://rag_user:rag_pass@postgres:5432/rag_db?sslmode=require
POSTGRES_USER=rag_user
POSTGRES_PASSWORD=rag_pass
POSTGRES_DB=rag_db

# Scanner Configuration
SCANNER_IMAGE=ventiapi-scanner
ZAP_IMAGE=ventiapi-zap
MAX_SCAN_DURATION=3600
MAX_REQUESTS_PER_SCAN=1000
DEFAULT_RATE_LIMIT=2.0

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json

# CORS - Set to your server's public IP/domain
ADDITIONAL_CORS_ORIGINS=http://YOUR_PUBLIC_IP:3001,http://YOUR_PUBLIC_IP:3000

# Frontend Public URLs - CHANGE YOUR_PUBLIC_IP to your server's IP
# These are baked into the Next.js build and sent to the browser
# NEXT_PUBLIC_SCANNER_SERVICE_URL=http://YOUR_PUBLIC_IP:8000
# Use relative path (empty string) to route via Nginx /api/
NEXT_PUBLIC_SCANNER_SERVICE_URL=
NEXT_PUBLIC_SCANNER_USERNAME=your-admin-username
NEXT_PUBLIC_SCANNER_PASSWORD=your-secure-password
NEXT_PUBLIC_MASTRA_URL=http://YOUR_PUBLIC_IP:4111

# OpenAI API Key (for Cedar/Mastra AI features)
OPENAI_API_KEY=sk-your-openai-api-key-here
MODEL=gpt-4o-mini

# Optional: Mistral API Key
MISTRAL_API_KEY=

# Optional: GitHub Token (for security advisory ingestion)
GITHUB_TOKEN=
